

export const SkillName = ({name}) => {
    return <button className="text-white text-center font-semibold bg-zinc-700 hover:text-palette px-5 py-1 mx-1 my-1 rounded-3xl ">
        {name}
    </button>
}